apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "finguard-chart.fullname" . }}-test-connection"
  labels:
    {{- include "finguard-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "finguard-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
